import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const eightThousanderSlice = createSlice({
  name: 'eightThousanderSlice',
  initialState,
  reducers: {}
})

export const { } = eightThousanderSlice.actions;

export default eightThousanderSlice.reducer;
